# 🔐 Secure Password Generator

A simple Python program to generate **strong, random passwords** using letters, numbers, and symbols. Styled with `rich` for a colorful terminal experience.  

---

## Features

- Custom password length (max 94 characters)  
- Mix of uppercase, lowercase, numbers, and symbols  
- Input validation to prevent errors  
- Progress bar while generating passwords  
- Option to generate multiple passwords in one session  

